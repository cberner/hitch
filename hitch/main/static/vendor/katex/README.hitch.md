# Vendored KaTeX

This directory contains the minified browser distribution and WOFF2 fonts from
KaTeX 0.18.4 (`katex@0.18.4` on npm). Hitch serves these files locally so
viewing a transcript does not require a third-party request.

The upstream license is in `LICENSE`. Only the runtime files needed by the
session transcript are included.
